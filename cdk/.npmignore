# Source - ship compiled lib/ only.
src/
test/
tsconfig.json
jest.config.js
node_modules/
coverage/
cdk.out/
*.tgz
.gitignore

# The status-lambda tsc output is superseded by the esbuild bundle
# (lib/status-lambda/bundle.js) that the construct references. Ship
# only the bundle + type declarations.
lib/status-lambda/index.js
