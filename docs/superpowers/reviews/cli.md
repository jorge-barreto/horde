# CLI Review

## BLOCKING
- None.

## WARNING
- [README.md:128-131] The `horde status --json` example shows a populated `tokens` object (non-zero input/output/cache/turns) on a run whose `status` is `"running"` AND whose `instance_id` is an **ECS task ARN** (`arn:aws:ecs:...`). This diff inserted the `tokens` line into that pre-existing example. It directly contradicts the behavior the same diff documents one paragraph later (README.md:137) and in SPEC.md / `horde docs json` (content.go:828-830): "ECS reports them at finalize" — an in-flight ECS run has NO token data, so its `tokens` object would be OMITTED, not populated. The example teaches consumers the opposite of the contract. Fix: either change the example's `instance_id` to a Docker container ID (Docker is the live-token path) or change `status` to a terminal value, or drop the `tokens` line from this specific running-ECS example.

## NOTE
- [cmd/horde/main.go:580,939] The live-telemetry guard is `run.TotalCostUSD == nil && (running|pending)`. Tokens are only read live when cost is also nil. This is correct given the current data model (Finalize is a no-op for running Docker runs, so cost and tokens are both nil together pre-finalize, and `fetchLiveTelemetry` reads both from one `costs.json` read), but the two are now coupled: a hypothetical state with cost set and tokens nil on a still-running run would never live-fetch tokens. The inner `if run.Tokens == nil` re-guard before overwrite is correct and defensive. No change needed; flagging the coupling.
- [internal/docs/content.go:126] The `horde docs getting-started` walkthrough still summarizes `horde results` as "status, total cost, duration, and a per-phase breakdown" — it omits token usage, even though `printFullResults` now prints a "Total Tokens" line and the `results` command `Description` was updated to mention token usage. Minor stale prose in the quickstart topic; the dedicated `horde docs json` topic was correctly updated.
- [cmd/horde/jsonv1_test.go:108] No test asserts `summary.tokens` is OMITTED (key absent) when no run in the set carries token data. The omission path is correct (nil `*TokensV1` + `omitempty`) and exercised implicitly via `SummaryEmpty`, but unlike the `total_cost_usd`/`runs:[]` assertions there is no explicit "tokens key absent" check. `TestListToV1_SummarySumsTokens` only covers the present case. Minor coverage gap.
- [cmd/horde/jsonv1_test.go:130,141,154] The three new token tests (`TestStatusToV1_Tokens`, `TestStatusToV1_NoTokens`, `TestListToV1_SummarySumsTokens`) are not marked `t.Parallel()`, while the adjacent label tests are. They mutate no env, so they could be parallel — minor consistency nit only.
- [cmd/horde/jsonv1.go:224] `summaryTokens(any bool, ...)` shadows the predeclared identifier `any`. Harmless and unambiguous here, but `present`/`hasTokens` would avoid the shadow.
- Contract integrity verified: `tokens` is a `*TokensV1` with `json:"tokens,omitempty"` on all four parents (StatusV1, ListRunV1, ListSummary, ResultsV1) and is nil/omitted when no data. JSON field tags (`input`/`output`/`cache_creation`/`cache_read`/`turns`) match across TokensV1, README, SPEC, and `horde docs json`. orc field names (`total_cache_creation_input_tokens` etc.) match between `liveCosts`, the provider `costsFile`/`RunResult`, and ORC_CONTRACT_EXPECTATIONS.md. No `fetchLiveCost` references remain in code (only in the plan doc). `resultsTokens` precedence is correct (stored `run.Tokens` first, then `fullRunResult` forward fields). `listToV1` token accumulator sums correctly and skips nil-token runs (the no-token run does not corrupt the sum or force a non-nil summary). No `Required: true` flags introduced; the ExitErrHandler error-envelope path is untouched. `go build`, `go vet`, and `go test ./cmd/horde/` are clean.
