# Store Review

## BLOCKING
None.

## WARNING
- [internal/store/sqlite_test.go:19-98] `TestNewSQLiteStore_MigratesLegacyDB` was NOT extended for the token columns. It still starts from the pre-labels 15-column `legacyDDL` and only asserts the `labels` column is added (line 79-81). It does not assert that the 5 new token columns (`input_tokens`/`output_tokens`/`cache_creation_tokens`/`cache_read_tokens`/`turns`) are added by `ensureColumns`, nor that a row written into a migrated pre-token DB reads back `Tokens == nil`. The migration code is correct (all 5 are in the `additive` slice at sqlite.go:115-122, and a legacy DB lacking them gets all six ALTERs), and the path is exercised indirectly by `TestNewSQLiteStore_CorrectColumns` (fresh open + ensureColumns yields the full 21-column set) plus the conformance `NilOptionalFields` case (Tokens nil round-trips). But the explicit "old on-disk DB gains the token columns and old token-less rows survive" assertion that exists for `labels` has no token equivalent. Suggest adding token-column checks to the legacy migration test, or a token-specific round-trip on the migrated DB.

## NOTE
- [internal/store/sqlite.go:178-205, 244-266] INSERT column list, the 21 `?` placeholders, the bind-arg order, and `scanRun`'s positional scan all align exactly (id…total_cost_usd, then input/output/cache_creation/cache_read/turns). The 5 token columns are appended in the identical order in all six occurrences — INSERT (182), GetRun (217), ListByRepo (412), ListRuns (452), FindActiveByTicket (493), ListActive (532). No positional mismatch.
- [internal/store/sqlite.go:46-68, 88-132] Token columns are present in BOTH the CREATE TABLE ddl and the idempotent `ensureColumns` additive list, so fresh and pre-existing DBs converge. Idempotency is guarded by the `existing[col.name]` check.
- [internal/store/sqlite.go:168-175] CreateRun sets the nullable `*int` locals only when `run.Tokens != nil`, so nil Tokens → NULL columns (not 0). UpdateRun (357-372) appends the token set-clauses only when `update.Tokens != nil`. Both correct.
- [internal/store/sqlite.go:300-308] The `inputTokens.Valid || outputTokens.Valid || …` guard correctly yields `Tokens == nil` for an all-NULL row and a non-nil struct (with 0s for any individual NULL) when any column is present. Matches the "zero is meaningful, nil = not known" semantics in store.go:82-93.
- [internal/store/dynamo.go:75-81] CreateRun writes the 5 token attributes (as `AttributeValueMemberN`) only when `run.Tokens != nil`. UpdateRun (360-373) writes them only when `update.Tokens != nil`. parseRun (228-269) uses the `anyToken` guard so absent attributes → `Tokens == nil`, and per-attribute `parseTokenAttr` defends type/parse errors with `%w` wrapping. Backward compat for old items (absent attrs) is correct.
- [internal/store/dynamo_schema.go:27-31, 49-56] The 5 new `Attr*` consts are added; correctly NOT added to `AttributeDefinitions` (they are not key/GSI attributes — DynamoDB rejects definitions for non-key attributes). Values are N (number) type via `strconv.Itoa`.
- [internal/store/dynamo.go] No string interpolation in token paths; all values flow through `AttributeValueMemberN`. SQLite token paths are fully parameterized — the only `fmt.Sprintf` in SQL (sqlite.go:127) is the ensureColumns ALTER on a hardcoded literal slice, unchanged pattern from the pre-existing `labels` column. RFC3339 timestamp handling is untouched by this diff.
- [internal/store/conformance_test.go:330-336, 388-393] AllFields sets Tokens and asserts non-nil + `*got.Tokens == *run.Tokens`. NilOptionalFields (426-428) and UpdateRun/SingleField (569-571) assert `Tokens == nil`. UpdateRun/MultipleFields (593-632) round-trips Tokens via UpdateRun. All run against BOTH SQLite and DynamoDB via `RunStoreConformance`. Coverage is complete for set/nil/update across both stores. Note: no conformance case writes a Tokens struct with all-zero values to pin that all-zero (input=output=…=0) still reads back as a non-nil struct rather than nil — a minor gap, since the production write path always sets all five together and parseRun/scanRun key presence on attribute/column presence (not zero-ness), so an all-zero write correctly round-trips non-nil. Optional to add.
- [SPEC.md:498-553] The SQLite and DynamoDB data-model tables were NOT updated to list the 5 new token columns/attributes, though the v0.2 status-Lambda prose (SPEC.md:81) and the `horde list` summary prose (169-181) were. Documentation-only; the store code matches CLAUDE.md's architecture note. Consider adding the rows for completeness.
